\standaloneconfig{
  multi=false,
  crop,
  convert={
    convertexe=convert,
    outext=.png,
    command={%
      \convertexe\space
      -density 300 -alpha remove -alpha off
      \infile\space
      --out \outfile
    }
  }
}
