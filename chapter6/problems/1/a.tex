These procedures do not always create the same heaps for the same input array.
\refFigure{6-1} shows the results of running each procedure on the input array $A=\langle1,2,3\rangle$.
\begin{figure}[htb]
    \captionsetup[subfigure]{}
    \subcaptionbox{\label{fig:6-1a}}[0.5\textwidth]{\subimport{figures/a/}{a.tikz}}
    \subcaptionbox{\label{fig:6-1b}}[0.5\textwidth]{\subimport{figures/a/}{b.tikz}}
    \caption{Comparison of the heaps built by both procedures for the same input array.\,
    \textbf{(a)}\, The heap created by \proc{Build-Max-Heap}.\,
    \textbf{(b)}\, The heap created by \proc{Build-Max-Heap}$'$.} \label{fig:6-1}
\end{figure}
