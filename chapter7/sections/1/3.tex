When \proc{Partition} is running on a subarray $A[p\subarr r]$ of length $n=r-p+1$, the \kw{for} loop performs exactly $n-1$ iterations, each taking a constant time.
Hence, the running time of \proc{Partition} is $\Theta(n)$.
