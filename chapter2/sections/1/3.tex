In the \proc{Insertion-Sort} procedure we can just reverse the second inequality in the \kw{while} loop condition.
In other words, replace line 5 by:

\begin{codebox}
\setcounter{codelinenumber}{4}
\zi    \While $j>0$ and $A[j]<\id{key}$
\end{codebox}
