Let's again consider $X$ and a similar sequence of Bernoulli trials to the one from part (b).
Here a success occurs with probability $p=k/n$, so the expected number of indices picked in \proc{Random-Search} before $x$ is found, is $\E{X}=1/p=n/k$.
